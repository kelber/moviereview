
<% if !user_signed_in? %>
<div class="jumbtron">
<div class="well">
<h1>Welcome to Movies Review</h1>
<%= link_to 'Sign Up', new_user_registration_path, class: "btn btn-primary btn-lg" %>
</div>
</div>



 <div class="panel panel-default">
    <div class="panel-body">
      <div class="row">

<% else %>  
<h1>Listing movies</h1> 

</div>
</div>
</div>
  
<% end %>

  <% @movies.each do |movie| %>
  <div class="col-sm-6 col-md-3">
  <div class="thumbnail">
    <div id="image">
    <%= link_to (image_tag movie.image.thumb('300x400#').url,class: "image"), movie if movie.image_stored? %>
     </div>
<hr>
  <h3><%= movie.title %></h3>
         <%= movie.description %>
          <%= movie.movie_length %>
          <%= movie.director %>
          <%= movie.rating %>

          
  </div>
  </div>
       <% end %>





